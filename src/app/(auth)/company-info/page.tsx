import { Metadata } from "next";
import ClientPage from "./CompanyInfoClientPage";

export const metadata: Metadata = {
  title: "Accounting  | Register",
};

const page = () => <ClientPage />;

export default page;
